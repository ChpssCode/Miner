---------------------------------------------------------------------------------------------------

Show/Unshow Object
this.input.keyboard.on('keydown-E', () => {
            text.water.setVisible(false);
            text.speed.setVisible(false); 
  });

---------------------------------------------------------------------------------------------------

Lose Oxygen when speed

while(speed > 0) {
  setInterval(() => this.oxygen -= 1, 1000]);
}

-------------------------------------------------------------------------------------------------

  localStorage.removeItem('oxygen', 0);
  localStorage.removeItem('hydrogen', 0);
  localStorage.removeItem('water', 0);
  localStorage.removeItem('fuel', 2000);
  localStorage.removeItem('counter', 0);
  localStorage.setItem("gameData", JSON.stringify({'oxygen': 0, 'hydrogen': 0, 'water': 0, 'fuel': 2000, 'counter': 0}))