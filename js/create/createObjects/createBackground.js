/**
 * @brief Creates the background object + Physics
 * @param refer refers to the 'create' function context
 */

function createBackground(refer) {
 refer.background = refer.add.tileSprite(2000, 2000, 4000, 4000, "background");
}